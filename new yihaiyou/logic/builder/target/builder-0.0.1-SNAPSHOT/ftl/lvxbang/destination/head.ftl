
    <title>${area.name}旅游_${area.name}旅行_${area.name}游记-旅行帮</title>
    <meta name="keywords"
          content="热门${area.name}游记, 热门${area.name}旅游线路, ${area.name}必游景点, ${area.name}必吃美食，最新${area.name}旅游攻略 "/>
    <meta name="description"
          content="2016年${area.name}热门旅游线路：介绍了${area.name}必游景点、酒店、必吃美食、热门游记等${area.name}旅游信息，搜索制作${area.name}旅游线路来旅行帮。"/>